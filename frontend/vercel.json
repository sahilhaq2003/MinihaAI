{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "NODE_OPTIONS='--max-old-space-size=4096' npx --yes vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}